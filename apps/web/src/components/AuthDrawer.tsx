// Every time the user carries out a privileged action while unauthenticated,
// we should trigger this drawer.

import { Drawer } from './ui/drawer';

const AuthDrawer = () => {
	return <Drawer></Drawer>;
};

export default AuthDrawer;
