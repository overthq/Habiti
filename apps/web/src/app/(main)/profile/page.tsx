'use client';

import Header from '@/components/home/Header';

const ProfilePage = () => {
	return (
		<div>
			<Header />
			<h1>Profile</h1>
		</div>
	);
};

export const runtime = 'edge';

export default ProfilePage;
