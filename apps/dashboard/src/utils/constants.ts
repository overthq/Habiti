// Apple App Review compliance: keep these false in shipped builds.
// Account creation and store creation must happen on the web at
// `${env.webFrontendUrl}/sell`.
export const STORE_CREATION_ENABLED: boolean = false;
export const ACCOUNT_CREATION_ENABLED: boolean = false;
