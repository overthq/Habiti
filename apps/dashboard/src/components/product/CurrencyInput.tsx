import React from 'react';
import { TextInput } from 'react-native';

// This input component keeps the "value" as lower denomination
// (kobo, cents), while showing the value as formatted (naira, dollars).

const CurrencyInput = () => {
	return <TextInput />;
};

export default CurrencyInput;
