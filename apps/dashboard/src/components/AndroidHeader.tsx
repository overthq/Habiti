import { View, Text } from 'react-native';

interface AndroidHeaderProps {
	hasBackButton?: boolean;
}

const AndroidHeader = ({ hasBackButton = true }: AndroidHeaderProps) => {
	return <View></View>;
};

export default AndroidHeader;
