import { Typography } from '@market/components';
import React from 'react';
import { View } from 'react-native';

const RecentSearches = () => {
	return (
		<View>
			<Typography weight='medium' variant='label' size='large'>
				Recent searches
			</Typography>
		</View>
	);
};

export default RecentSearches;
