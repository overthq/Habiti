import React from 'react';
import { View } from 'react-native';
import Button from '../global/Button';

const ProductsFilterSheet = () => {
	return (
		<View>
			<Button text='Apply' />
		</View>
	);
};

export default ProductsFilterSheet;
