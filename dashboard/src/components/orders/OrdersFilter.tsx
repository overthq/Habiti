import React from 'react';
import { View, StyleSheet } from 'react-native';

// The general idea behind this component
// is to enable filtering/sorting of orders by status, amount etc.
// What exactly this will look like is yet to be determined.

const OrdersFilter = () => {
	return <View style={styles.container}></View>;
};

const styles = StyleSheet.create({
	container: {}
});

export default OrdersFilter;
