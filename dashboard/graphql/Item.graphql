mutation CreateItem($input: ItemInput!) {
	createItem(input: $input) {
		_id
		name
		description
		pricePerUnit
		featured
		unit
	}
}

query StoreItems {
	storeItems {
		_id
		name
		description
		pricePerUnit
		featured
		unit
	}
}

