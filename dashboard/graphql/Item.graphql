mutation CreateItem($input: ItemInput!) {
	createItem(input: $input) {
		_id
		name
		pricePerUnit
		featured
		unit
	}
}

query StoreItems {
	storeItems {
		_id
		name
		pricePerUnit
		featured
		unit
	}
}

query Items {
	items {
		_id
		name
		pricePerUnit
		featured
		unit
	}
}
