overwrite: true
schema: http://localhost:8080/v1/graphql
documents: graphql/*.graphql
generates:
  src/types/api-types.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
    config:
      withHooks: true
      withComponent: false
