query Search($searchTerm: String!) {
	stores(where: { name: { _ilike: $searchTerm } }, order_by: [{ name: asc }]) {
		name
		short_name
	}

	items(where: { name: { _ilike: $searchTerm } }, order_by: [{ name: asc }]) {
		name
	}
}
