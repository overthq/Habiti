query Items {
	items {
		id
		name
		store_id
		featured
		price_per_unit
		unit
	}
}

query StoreItems($storeId: ID!) {
	storeItems: items(where: { store_id: { equal: $storeId } }) {
		id
		name
		store_id
		featured
		price_per_unit
		unit
	}
}
