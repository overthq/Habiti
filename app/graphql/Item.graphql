query Items {
	items {
		_id
		name
		storeId
		featured
		pricePerUnit
		unit
	}
}

query StoreItems($storeId: ID!) {
	storeItems(storeId: $storeId) {
		_id
		name
		storeId
		featured
		pricePerUnit
		unit
	}
}
