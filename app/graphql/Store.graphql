query Stores {
	stores {
		id
		name
	}
}

query Store($storeId: ID!) {
	store(id: $storeId) {
		id
		name
		description
		website
		twitter
		instagram
		followers {
			follower {
				id
				name
			}
		}
	}
}
