query Stores {
	stores {
		_id
		name
		websiteUrl
		instagramUsername
		twitterUsername
	}
}

query Store($storeId: ID!) {
	store(storeId: $storeId) {
		_id
		name
		websiteUrl
		instagramUsername
		twitterUsername
	}
}
