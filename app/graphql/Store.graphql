query Stores {
	stores {
		id
		name
		image {
			id
			path
		}
	}
}

query Store($storeId: ID!) {
	store(id: $storeId) {
		id
		name
		description
		website
		twitter
		instagram
		followers {
			follower {
				id
				name
			}
		}
	}
}
