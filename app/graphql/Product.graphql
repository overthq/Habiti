query StoreProducts($storeId: ID!) {
	store(id: $storeId) {
		products {
			id
			name
			description
			unitPrice
		}
	}
}

query Product($productId: ID!) {
	product(id: $productId) {
		id
		name
		description
		unitPrice
		storeId
	}
}
