query UserOrders {
	userOrders {
		_id
		status
		store {
			name
		}
		cart {
			item {
				name
				pricePerUnit
			}
			quantity
		}
	}
}

mutation PlaceOrder($storeId: ID!, $input: OrderInput!) {
	_id
	status
	store {
		name
	}
	cart {
		item {
			name
			pricePerUnit
		}
		quantity
	}
}
